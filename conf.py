# Base directory where all files will be stored
BASE_DIR = "/home/hdd/uploads"

# Server configuration
HOST = "0.0.0.0"  # Listen on all interfaces
PORT = 8050

# Number of files to load per request (pagination)
FILES_PER_REQUEST = 10

# Debug mode (set to True for development)
DEBUG = False

# Default language if browser language cannot be detected
DEFAULT_LANGUAGE = "en"